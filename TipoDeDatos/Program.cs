
namespace TipoDeDatos
{
    class Program
    {
        static void Main(string[] args)
        {
            // int
            int age = 30;
            // double
            double price = 19.95;
            // bool
            bool found = false;
            // char
            char letter = 'A';
            // string
            string name = "Bob";
            // decimal
            decimal total = 25.95m;
            // DateTime
            System.DateTime today = System.DateTime.Now;
            // TimeSpan
            System.TimeSpan duration = new System.TimeSpan(1, 12, 23, 62);
            // Guid
            System.Guid uniqueId = System.Guid.NewGuid();
            
            System.Console.WriteLine(age);
            
            
        }
    }
}

